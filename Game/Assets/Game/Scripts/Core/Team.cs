namespace Core
{
    /// <summary>
    /// Identifies which team a game object belongs to.
    /// Used by TowerController, NexusController, MinionController, PlayerSpawner, etc.
    /// </summary>
    public enum Team
    {
        Blue = 0,
        Red  = 1,
    }
}
